Copy fork of project into my own repository:
https://github.com/DWH-course-examples/dbt_clickhouse_lab.git

Make new local directory for the project:
  mkdir dbt_project
Enter project directory:
  cd dbt_project
Git initialisation:
  git init
Clone remote repository:
  git clone git@github.com:DWH-creation/dbt-datavault-postgres-06-09.git
Check all remote repositories:
  git remote -v
Enter directory dbt-datavault-postgres-06-09:
  cd dbt-datavault-postgres-06-09
Up container from the project docker-compose.yml:
  docker compose up
Check all the containers:
  docker ps -a
Start containers if are not running:
  docker start <ID postgres container>
  docker start <ID devcontainer>
Check postgres and devcontainer containers are running:
  docker ps
Check that dbt sees postrges. Enter command line of postgres container:
  docker exec -it <ID postgres container> /bin/bash
Install dbt-core and dbt-postgres packages:
  sudo apt install python3.10-venv
  cd .venv/bin
  sudo apt install python3-virtualenv
  virtualenv .venv
  source .venv/bin/activate
  pip install dbt-core dbt-postgres
If we need only upgrade dbt-core, dbt-postgres:
  pip install --upgrade dbt-postgres dbt-core
Clean dependancies:
  dbt clean
Disable check of certificates:
  git config --global http.ssIVerify false
Install packages from packages.yml:
  dbt deps
Check connection between dbt and postgres:
  dbt debug
Place data from source:
  dbt seed
Run all transformations from sql models:
  dbt run
Go to postgres-container terminal:
  docker exec -it <container ID> /bin/bash
Go to postgresql.conf:
  /var/lib/postgresql/18/docker#
Install nano:
  apt-get update
  apt-get install nano
Open postgresql.conf
  nano postgresql.conf
  listen adresses = "*"
  port = 5432
  cntr + O //safe
  cntr + x //exit 
Open pg_hba.conf
  nano pg_hba.conf
  host all all 0.0.0.0/0 md5 //type this to the end of file
  cntr + O //safe
  cntr + x //exit  
Restart postgres container:
  cntr + p -> cntr + Q //exit and keep container running
  docker restart <container ID>
